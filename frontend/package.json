{
  "name": "linguafolio-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "live-server --port=5500 --host=127.0.0.1 ./",
    "build": "node scripts/generate-config.js"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
